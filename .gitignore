# 仓库级忽略规则：只处理“不属于任何一件作品”的系统噪音。
# 各作品自己的依赖、构建产物和缓存，由该作品目录内的 .gitignore 负责。
#
# 注意：不含斜杠的模式在任意层级生效，所以下面一行可覆盖所有子目录。

# macOS
.DS_Store

# Windows
Thumbs.db

# 工具输出（如 Playwright MCP 的截图与控制台日志）
.playwright-mcp/

# 本地构建中转目录：站点产物与镜像 tar，不进版本库
/build/

# 本地私人笔记，不进版本库。
# 用斜杠锚定到仓库根目录，避免误伤各作品目录内的同名文件。
/*.local.md

.gstack/
*/superpowers/