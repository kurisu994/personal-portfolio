# 构建上下文从仓库根目录开始。这里只排除与镜像无关的内容，
# 因为 Docker 会把整个上下文打包发给守护进程，排除项直接决定构建快慢。
# 注意：不要排除 migration/ 与 migration-showcase/src 等源码目录。

# ── 版本控制 ──────────────────────────────────────────────────────────
.git
.gitignore

# ── 依赖与产物 ────────────────────────────────────────────────────────
# 容器内会重新安装并构建。宿主机 node_modules 里含 macOS 平台二进制
# （esbuild / rsbuild 按平台分发），拷进 Linux 镜像必然运行失败。
**/node_modules
**/dist
**/.pnpm-store
**/*.tsbuildinfo

# ── 验收证据与调试产物 ────────────────────────────────────────────────
# 体积大且不参与部署，单独保存在 migration-showcase/artifacts/。
migration-showcase/artifacts

# ── 系统与编辑器噪音 ──────────────────────────────────────────────────
**/.DS_Store
**/*.log
**/.idea
**/.vscode
